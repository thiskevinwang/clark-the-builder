// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AddOrganizationTeamMemberData, AddOrganizationTeamMemberErrors, AddOrganizationTeamMemberResponses, CancelBouncerResizeRequestData, CancelBouncerResizeRequestErrors, CancelBouncerResizeRequestResponses, CancelBranchChangeRequestData, CancelBranchChangeRequestErrors, CancelBranchChangeRequestResponses, CancelDeployRequestData, CancelDeployRequestErrors, CancelDeployRequestResponses, CloseDeployRequestData, CloseDeployRequestErrors, CloseDeployRequestResponses, CompleteErroredDeployData, CompleteErroredDeployErrors, CompleteErroredDeployResponses, CompleteGatedDeployRequestData, CompleteGatedDeployRequestErrors, CompleteGatedDeployRequestResponses, CompleteRevertData, CompleteRevertErrors, CompleteRevertResponses, CreateBackupData, CreateBackupErrors, CreateBackupResponses, CreateBouncerData, CreateBouncerErrors, CreateBouncerResponses, CreateBranchData, CreateBranchErrors, CreateBranchResponses, CreateDatabaseData, CreateDatabaseErrors, CreateDatabasePostgresCidrData, CreateDatabasePostgresCidrErrors, CreateDatabasePostgresCidrResponses, CreateDatabaseResponses, CreateDeployRequestData, CreateDeployRequestErrors, CreateDeployRequestResponses, CreateKeyspaceData, CreateKeyspaceErrors, CreateKeyspaceResponses, CreateOauthTokenData, CreateOauthTokenErrors, CreateOauthTokenResponses, CreateOrganizationTeamData, CreateOrganizationTeamErrors, CreateOrganizationTeamResponses, CreatePasswordData, CreatePasswordErrors, CreatePasswordResponses, CreateQueryPatternsReportData, CreateQueryPatternsReportErrors, CreateQueryPatternsReportResponses, CreateRoleData, CreateRoleErrors, CreateRoleResponses, CreateServiceTokenData, CreateServiceTokenErrors, CreateServiceTokenResponses, CreateWebhookData, CreateWebhookErrors, CreateWebhookResponses, CreateWorkflowData, CreateWorkflowErrors, CreateWorkflowResponses, DeleteBackupData, DeleteBackupErrors, DeleteBackupResponses, DeleteBouncerData, DeleteBouncerErrors, DeleteBouncerResponses, DeleteBranchData, DeleteBranchErrors, DeleteBranchResponses, DeleteDatabaseData, DeleteDatabaseErrors, DeleteDatabasePostgresCidrData, DeleteDatabasePostgresCidrErrors, DeleteDatabasePostgresCidrResponses, DeleteDatabaseResponses, DeleteKeyspaceData, DeleteKeyspaceErrors, DeleteKeyspaceResponses, DeleteOauthTokenData, DeleteOauthTokenErrors, DeleteOauthTokenResponses, DeleteOrganizationTeamData, DeleteOrganizationTeamErrors, DeleteOrganizationTeamResponses, DeletePasswordData, DeletePasswordErrors, DeletePasswordResponses, DeleteQueryPatternsReportData, DeleteQueryPatternsReportErrors, DeleteQueryPatternsReportResponses, DeleteRoleData, DeleteRoleErrors, DeleteRoleResponses, DeleteServiceTokenData, DeleteServiceTokenErrors, DeleteServiceTokenResponses, DeleteWebhookData, DeleteWebhookErrors, DeleteWebhookResponses, DemoteBranchData, DemoteBranchErrors, DemoteBranchResponses, DisableSafeMigrationsData, DisableSafeMigrationsErrors, DisableSafeMigrationsResponses, EnableSafeMigrationsData, EnableSafeMigrationsErrors, EnableSafeMigrationsResponses, GetBackupData, GetBackupErrors, GetBackupResponses, GetBouncerData, GetBouncerErrors, GetBouncerResponses, GetBranchChangeRequestData, GetBranchChangeRequestErrors, GetBranchChangeRequestResponses, GetBranchData, GetBranchErrors, GetBranchResponses, GetBranchSchemaData, GetBranchSchemaErrors, GetBranchSchemaResponses, GetCurrentUserData, GetCurrentUserErrors, GetCurrentUserResponses, GetDatabaseData, GetDatabaseErrors, GetDatabasePostgresCidrData, GetDatabasePostgresCidrErrors, GetDatabasePostgresCidrResponses, GetDatabaseResponses, GetDatabaseThrottlerData, GetDatabaseThrottlerErrors, GetDatabaseThrottlerResponses, GetDefaultRoleData, GetDefaultRoleErrors, GetDefaultRoleResponses, GetDeploymentData, GetDeploymentErrors, GetDeploymentResponses, GetDeployQueueData, GetDeployQueueErrors, GetDeployQueueResponses, GetDeployRequestData, GetDeployRequestErrors, GetDeployRequestResponses, GetDeployRequestThrottlerData, GetDeployRequestThrottlerErrors, GetDeployRequestThrottlerResponses, GetInvoiceData, GetInvoiceErrors, GetInvoiceLineItemsData, GetInvoiceLineItemsErrors, GetInvoiceLineItemsResponses, GetInvoiceResponses, GetKeyspaceData, GetKeyspaceErrors, GetKeyspaceResponses, GetKeyspaceRolloutStatusData, GetKeyspaceRolloutStatusErrors, GetKeyspaceRolloutStatusResponses, GetKeyspaceVschemaData, GetKeyspaceVschemaErrors, GetKeyspaceVschemaResponses, GetOauthApplicationData, GetOauthApplicationErrors, GetOauthApplicationResponses, GetOauthTokenData, GetOauthTokenErrors, GetOauthTokenResponses, GetOrganizationData, GetOrganizationErrors, GetOrganizationMembershipData, GetOrganizationMembershipErrors, GetOrganizationMembershipResponses, GetOrganizationResponses, GetOrganizationTeamData, GetOrganizationTeamErrors, GetOrganizationTeamMemberData, GetOrganizationTeamMemberErrors, GetOrganizationTeamMemberResponses, GetOrganizationTeamResponses, GetPasswordData, GetPasswordErrors, GetPasswordResponses, GetQueryPatternsReportData, GetQueryPatternsReportErrors, GetQueryPatternsReportStatusData, GetQueryPatternsReportStatusErrors, GetQueryPatternsReportStatusResponses, GetRoleData, GetRoleErrors, GetRoleResponses, GetServiceTokenData, GetServiceTokenErrors, GetServiceTokenResponses, GetWebhookData, GetWebhookErrors, GetWebhookResponses, GetWorkflowData, GetWorkflowErrors, GetWorkflowResponses, LintBranchSchemaData, LintBranchSchemaErrors, LintBranchSchemaResponses, ListAuditLogsData, ListAuditLogsErrors, ListAuditLogsResponses, ListBackupsData, ListBackupsErrors, ListBackupsResponses, ListBouncerResizeRequestsData, ListBouncerResizeRequestsErrors, ListBouncerResizeRequestsResponses, ListBouncersData, ListBouncersErrors, ListBouncersResponses, ListBranchBouncerResizeRequestsData, ListBranchBouncerResizeRequestsErrors, ListBranchBouncerResizeRequestsResponses, ListBranchChangeRequestsData, ListBranchChangeRequestsErrors, ListBranchChangeRequestsResponses, ListBranchesData, ListBranchesErrors, ListBranchesResponses, ListClusterSizeSkusData, ListClusterSizeSkusErrors, ListClusterSizeSkusResponses, ListDatabasePostgresCidrsData, ListDatabasePostgresCidrsErrors, ListDatabasePostgresCidrsResponses, ListDatabaseRegionsData, ListDatabaseRegionsErrors, ListDatabaseRegionsResponses, ListDatabasesData, ListDatabasesErrors, ListDatabasesResponses, ListDeployOperationsData, ListDeployOperationsErrors, ListDeployOperationsResponses, ListDeployRequestReviewsData, ListDeployRequestReviewsErrors, ListDeployRequestReviewsResponses, ListDeployRequestsData, ListDeployRequestsErrors, ListDeployRequestsResponses, ListExtensionsData, ListExtensionsErrors, ListExtensionsResponses, ListGeneratedQueryPatternsReportsData, ListGeneratedQueryPatternsReportsErrors, ListGeneratedQueryPatternsReportsResponses, ListInvoicesData, ListInvoicesErrors, ListInvoicesResponses, ListKeyspacesData, ListKeyspacesErrors, ListKeyspacesResponses, ListOauthApplicationsData, ListOauthApplicationsErrors, ListOauthApplicationsResponses, ListOauthTokensData, ListOauthTokensErrors, ListOauthTokensResponses, ListOrganizationMembersData, ListOrganizationMembersErrors, ListOrganizationMembersResponses, ListOrganizationsData, ListOrganizationsErrors, ListOrganizationsResponses, ListOrganizationTeamMembersData, ListOrganizationTeamMembersErrors, ListOrganizationTeamMembersResponses, ListOrganizationTeamsData, ListOrganizationTeamsErrors, ListOrganizationTeamsResponses, ListParametersData, ListParametersErrors, ListParametersResponses, ListPasswordsData, ListPasswordsErrors, ListPasswordsResponses, ListPublicRegionsData, ListPublicRegionsErrors, ListPublicRegionsResponses, ListReadOnlyRegionsData, ListReadOnlyRegionsErrors, ListReadOnlyRegionsResponses, ListRegionsForOrganizationData, ListRegionsForOrganizationErrors, ListRegionsForOrganizationResponses, ListRolesData, ListRolesErrors, ListRolesResponses, ListServiceTokensData, ListServiceTokensErrors, ListServiceTokensResponses, ListWebhooksData, ListWebhooksErrors, ListWebhooksResponses, ListWorkflowsData, ListWorkflowsErrors, ListWorkflowsResponses, PromoteBranchData, PromoteBranchErrors, PromoteBranchResponses, QueueDeployRequestData, QueueDeployRequestErrors, QueueDeployRequestResponses, ReassignRoleObjectsData, ReassignRoleObjectsErrors, ReassignRoleObjectsResponses, RemoveOrganizationMemberData, RemoveOrganizationMemberErrors, RemoveOrganizationMemberResponses, RemoveOrganizationTeamMemberData, RemoveOrganizationTeamMemberErrors, RemoveOrganizationTeamMemberResponses, RenewPasswordData, RenewPasswordErrors, RenewPasswordResponses, RenewRoleData, RenewRoleErrors, RenewRoleResponses, ResetDefaultRoleData, ResetDefaultRoleErrors, ResetDefaultRoleResponses, ResetRoleData, ResetRoleErrors, ResetRoleResponses, ReviewDeployRequestData, ReviewDeployRequestErrors, ReviewDeployRequestResponses, SkipRevertPeriodData, SkipRevertPeriodErrors, SkipRevertPeriodResponses, TestWebhookData, TestWebhookErrors, TestWebhookResponses, UpdateAutoApplyData, UpdateAutoApplyErrors, UpdateAutoApplyResponses, UpdateAutoDeleteBranchData, UpdateAutoDeleteBranchErrors, UpdateAutoDeleteBranchResponses, UpdateBackupData, UpdateBackupErrors, UpdateBackupResponses, UpdateBouncerResizeRequestData, UpdateBouncerResizeRequestErrors, UpdateBouncerResizeRequestResponses, UpdateBranchChangeRequestData, UpdateBranchChangeRequestErrors, UpdateBranchChangeRequestResponses, UpdateBranchClusterConfigData, UpdateBranchClusterConfigErrors, UpdateBranchClusterConfigResponses, UpdateBranchData, UpdateBranchErrors, UpdateBranchResponses, UpdateDatabasePostgresCidrData, UpdateDatabasePostgresCidrErrors, UpdateDatabasePostgresCidrResponses, UpdateDatabaseSettingsData, UpdateDatabaseSettingsErrors, UpdateDatabaseSettingsResponses, UpdateDatabaseThrottlerData, UpdateDatabaseThrottlerErrors, UpdateDatabaseThrottlerResponses, UpdateDeployRequestThrottlerData, UpdateDeployRequestThrottlerErrors, UpdateDeployRequestThrottlerResponses, UpdateKeyspaceData, UpdateKeyspaceErrors, UpdateKeyspaceResponses, UpdateKeyspaceVschemaData, UpdateKeyspaceVschemaErrors, UpdateKeyspaceVschemaResponses, UpdateOrganizationData, UpdateOrganizationErrors, UpdateOrganizationMembershipData, UpdateOrganizationMembershipErrors, UpdateOrganizationMembershipResponses, UpdateOrganizationResponses, UpdateOrganizationTeamData, UpdateOrganizationTeamErrors, UpdateOrganizationTeamResponses, UpdatePasswordData, UpdatePasswordErrors, UpdatePasswordResponses, UpdateRoleData, UpdateRoleErrors, UpdateRoleResponses, UpdateWebhookData, UpdateWebhookErrors, UpdateWebhookResponses, VerifyWorkflowData, VerifyWorkflowErrors, VerifyWorkflowResponses, WorkflowCancelData, WorkflowCancelErrors, WorkflowCancelResponses, WorkflowCompleteData, WorkflowCompleteErrors, WorkflowCompleteResponses, WorkflowCutoverData, WorkflowCutoverErrors, WorkflowCutoverResponses, WorkflowRetryData, WorkflowRetryErrors, WorkflowRetryResponses, WorkflowReverseCutoverData, WorkflowReverseCutoverErrors, WorkflowReverseCutoverResponses, WorkflowReverseTrafficData, WorkflowReverseTrafficErrors, WorkflowReverseTrafficResponses, WorkflowSwitchPrimariesData, WorkflowSwitchPrimariesErrors, WorkflowSwitchPrimariesResponses, WorkflowSwitchReplicasData, WorkflowSwitchReplicasErrors, WorkflowSwitchReplicasResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * List organizations
 *
 * When using a service token, returns the list of organizations the service token has access to. When using an OAuth token, returns the list of organizations the user has access to.
 * ### Authorization
 * A   OAuth token must have at least one of the following   scopes in order to use this API endpoint:
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | User | `read_organizations` |
 */
export const listOrganizations = <ThrowOnError extends boolean = false>(options?: Options<ListOrganizationsData, ThrowOnError>) => (options?.client ?? client).get<ListOrganizationsResponses, ListOrganizationsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations',
    ...options
});

/**
 * Get an organization
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | User | `read_organizations` |
 * | Organization | `read_organization` |
 */
export const getOrganization = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationData, ThrowOnError>) => (options.client ?? client).get<GetOrganizationResponses, GetOrganizationErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}',
    ...options
});

/**
 * Update an organization
 *
 *
 * ### Authorization
 * A   OAuth token must have at least one of the following   scopes in order to use this API endpoint:
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_organization` |
 */
export const updateOrganization = <ThrowOnError extends boolean = false>(options: Options<UpdateOrganizationData, ThrowOnError>) => (options.client ?? client).patch<UpdateOrganizationResponses, UpdateOrganizationErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List audit logs
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_audit_logs`
 *
 *
 */
export const listAuditLogs = <ThrowOnError extends boolean = false>(options: Options<ListAuditLogsData, ThrowOnError>) => (options.client ?? client).get<ListAuditLogsResponses, ListAuditLogsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/audit-log',
    ...options
});

/**
 * List available cluster sizes
 *
 * List available cluster sizes for an organization
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | User | `read_organizations` |
 * | Organization | `read_organization` |
 */
export const listClusterSizeSkus = <ThrowOnError extends boolean = false>(options: Options<ListClusterSizeSkusData, ThrowOnError>) => (options.client ?? client).get<ListClusterSizeSkusResponses, ListClusterSizeSkusErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/cluster-size-skus',
    ...options
});

/**
 * List databases
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_database`, `delete_database`, `write_database`, `read_branch`, `delete_branch`, `create_branch`, `delete_production_branch`, `connect_branch`, `connect_production_branch`, `delete_branch_password`, `delete_production_branch_password`, `read_deploy_request`, `create_deploy_request`, `approve_deploy_request`, `read_comment`, `create_comment`, `restore_backup`, `restore_production_branch_backup`, `read_backups`, `write_backups`, `delete_backups`, `delete_production_branch_backups`, `write_branch_vschema`, `write_production_branch_vschema`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_databases` |
 */
export const listDatabases = <ThrowOnError extends boolean = false>(options: Options<ListDatabasesData, ThrowOnError>) => (options.client ?? client).get<ListDatabasesResponses, ListDatabasesErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases',
    ...options
});

/**
 * Create a database
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `create_databases`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `create_databases` |
 */
export const createDatabase = <ThrowOnError extends boolean = false>(options: Options<CreateDatabaseData, ThrowOnError>) => (options.client ?? client).post<CreateDatabaseResponses, CreateDatabaseErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a database
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `delete_databases` |
 * | Database | `delete_database` |
 */
export const deleteDatabase = <ThrowOnError extends boolean = false>(options: Options<DeleteDatabaseData, ThrowOnError>) => (options.client ?? client).delete<DeleteDatabaseResponses, DeleteDatabaseErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}',
    ...options
});

/**
 * Get a database
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_database`, `delete_database`, `write_database`, `read_branch`, `delete_branch`, `create_branch`, `delete_production_branch`, `connect_branch`, `connect_production_branch`, `delete_branch_password`, `delete_production_branch_password`, `read_deploy_request`, `create_deploy_request`, `approve_deploy_request`, `read_comment`, `create_comment`, `restore_backup`, `restore_production_branch_backup`, `read_backups`, `write_backups`, `delete_backups`, `delete_production_branch_backups`, `write_branch_vschema`, `write_production_branch_vschema`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_databases` |
 * | Database | `read_database` |
 */
export const getDatabase = <ThrowOnError extends boolean = false>(options: Options<GetDatabaseData, ThrowOnError>) => (options.client ?? client).get<GetDatabaseResponses, GetDatabaseErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}',
    ...options
});

/**
 * Update database settings
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const updateDatabaseSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateDatabaseSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateDatabaseSettingsResponses, UpdateDatabaseSettingsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List branches
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const listBranches = <ThrowOnError extends boolean = false>(options: Options<ListBranchesData, ThrowOnError>) => (options.client ?? client).get<ListBranchesResponses, ListBranchesErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches',
    ...options
});

/**
 * Create a branch
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `create_branch`, `restore_production_branch_backup`, `restore_backup`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_branches`, `restore_production_branch_backups`, `restore_backups` |
 * | Database | `write_branches`, `restore_production_branch_backups`, `restore_backups` |
 * | Branch | `restore_backups` |
 */
export const createBranch = <ThrowOnError extends boolean = false>(options: Options<CreateBranchData, ThrowOnError>) => (options.client ?? client).post<CreateBranchResponses, CreateBranchErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a branch
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `delete_branches`, `delete_production_branches` |
 * | Database | `delete_branches`, `delete_production_branches` |
 * | Branch | `delete_branch` |
 */
export const deleteBranch = <ThrowOnError extends boolean = false>(options: Options<DeleteBranchData, ThrowOnError>) => (options.client ?? client).delete<DeleteBranchResponses, DeleteBranchErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}',
    ...options
});

/**
 * Get a branch
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const getBranch = <ThrowOnError extends boolean = false>(options: Options<GetBranchData, ThrowOnError>) => (options.client ?? client).get<GetBranchResponses, GetBranchErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}',
    ...options
});

/**
 * Update a branch
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_branches` |
 * | Database | `write_branches` |
 * | Branch | `write_branch` |
 */
export const updateBranch = <ThrowOnError extends boolean = false>(options: Options<UpdateBranchData, ThrowOnError>) => (options.client ?? client).patch<UpdateBranchResponses, UpdateBranchErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List backups
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_backups`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_backups` |
 * | Database | `read_backups` |
 * | Branch | `read_backups` |
 */
export const listBackups = <ThrowOnError extends boolean = false>(options: Options<ListBackupsData, ThrowOnError>) => (options.client ?? client).get<ListBackupsResponses, ListBackupsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/backups',
    ...options
});

/**
 * Create a backup
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_backups`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_backups` |
 * | Database | `write_backups` |
 * | Branch | `write_backups` |
 */
export const createBackup = <ThrowOnError extends boolean = false>(options: Options<CreateBackupData, ThrowOnError>) => (options.client ?? client).post<CreateBackupResponses, CreateBackupErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/backups',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a backup
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_backups`, `delete_production_branch_backups`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `delete_backups`, `delete_production_branch_backups` |
 * | Database | `delete_backups`, `delete_production_branch_backups` |
 * | Branch | `delete_backups` |
 */
export const deleteBackup = <ThrowOnError extends boolean = false>(options: Options<DeleteBackupData, ThrowOnError>) => (options.client ?? client).delete<DeleteBackupResponses, DeleteBackupErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}',
    ...options
});

/**
 * Get a backup
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_backups`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_backups` |
 * | Database | `read_backups` |
 * | Branch | `read_backups` |
 */
export const getBackup = <ThrowOnError extends boolean = false>(options: Options<GetBackupData, ThrowOnError>) => (options.client ?? client).get<GetBackupResponses, GetBackupErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}',
    ...options
});

/**
 * Update a backup
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_backups`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_backups` |
 * | Database | `write_backups` |
 * | Branch | `write_backups` |
 */
export const updateBackup = <ThrowOnError extends boolean = false>(options: Options<UpdateBackupData, ThrowOnError>) => (options.client ?? client).patch<UpdateBackupResponses, UpdateBackupErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get bouncer resize requests
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const listBranchBouncerResizeRequests = <ThrowOnError extends boolean = false>(options: Options<ListBranchBouncerResizeRequestsData, ThrowOnError>) => (options.client ?? client).get<ListBranchBouncerResizeRequestsResponses, ListBranchBouncerResizeRequestsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes',
    ...options
});

/**
 * List bouncers
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const listBouncers = <ThrowOnError extends boolean = false>(options: Options<ListBouncersData, ThrowOnError>) => (options.client ?? client).get<ListBouncersResponses, ListBouncersErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/bouncers',
    ...options
});

/**
 * Create a bouncer
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const createBouncer = <ThrowOnError extends boolean = false>(options: Options<CreateBouncerData, ThrowOnError>) => (options.client ?? client).post<CreateBouncerResponses, CreateBouncerErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/bouncers',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a bouncer
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const deleteBouncer = <ThrowOnError extends boolean = false>(options: Options<DeleteBouncerData, ThrowOnError>) => (options.client ?? client).delete<DeleteBouncerResponses, DeleteBouncerErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}',
    ...options
});

/**
 * Get a bouncer
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const getBouncer = <ThrowOnError extends boolean = false>(options: Options<GetBouncerData, ThrowOnError>) => (options.client ?? client).get<GetBouncerResponses, GetBouncerErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}',
    ...options
});

/**
 * Cancel a resize request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const cancelBouncerResizeRequest = <ThrowOnError extends boolean = false>(options: Options<CancelBouncerResizeRequestData, ThrowOnError>) => (options.client ?? client).delete<CancelBouncerResizeRequestResponses, CancelBouncerResizeRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes',
    ...options
});

/**
 * Get bouncer resize requests
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const listBouncerResizeRequests = <ThrowOnError extends boolean = false>(options: Options<ListBouncerResizeRequestsData, ThrowOnError>) => (options.client ?? client).get<ListBouncerResizeRequestsResponses, ListBouncerResizeRequestsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes',
    ...options
});

/**
 * Upsert a bouncer resize request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const updateBouncerResizeRequest = <ThrowOnError extends boolean = false>(options: Options<UpdateBouncerResizeRequestData, ThrowOnError>) => (options.client ?? client).patch<UpdateBouncerResizeRequestResponses, UpdateBouncerResizeRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get branch change requests
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const listBranchChangeRequests = <ThrowOnError extends boolean = false>(options: Options<ListBranchChangeRequestsData, ThrowOnError>) => (options.client ?? client).get<ListBranchChangeRequestsResponses, ListBranchChangeRequestsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/changes',
    ...options
});

/**
 * Upsert a change request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const updateBranchChangeRequest = <ThrowOnError extends boolean = false>(options: Options<UpdateBranchChangeRequestData, ThrowOnError>) => (options.client ?? client).patch<UpdateBranchChangeRequestResponses, UpdateBranchChangeRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/changes',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get a branch change request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const getBranchChangeRequest = <ThrowOnError extends boolean = false>(options: Options<GetBranchChangeRequestData, ThrowOnError>) => (options.client ?? client).get<GetBranchChangeRequestResponses, GetBranchChangeRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}',
    ...options
});

/**
 * Change a branch cluster configuration
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 *
 */
export const updateBranchClusterConfig = <ThrowOnError extends boolean = false>(options: Options<UpdateBranchClusterConfigData, ThrowOnError>) => (options.client ?? client).patch<UpdateBranchClusterConfigResponses, UpdateBranchClusterConfigErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/cluster',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Demote a branch
 *
 * Demotes a branch from production to development
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `connect_production_branch`, `demote_branches`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `demote_branches` |
 * | Database | `demote_branches` |
 */
export const demoteBranch = <ThrowOnError extends boolean = false>(options: Options<DemoteBranchData, ThrowOnError>) => (options.client ?? client).post<DemoteBranchResponses, DemoteBranchErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/demote',
    ...options
});

/**
 * List cluster extensions
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const listExtensions = <ThrowOnError extends boolean = false>(options: Options<ListExtensionsData, ThrowOnError>) => (options.client ?? client).get<ListExtensionsResponses, ListExtensionsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/extensions',
    ...options
});

/**
 * Get keyspaces
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`
 *
 *
 */
export const listKeyspaces = <ThrowOnError extends boolean = false>(options: Options<ListKeyspacesData, ThrowOnError>) => (options.client ?? client).get<ListKeyspacesResponses, ListKeyspacesErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/keyspaces',
    ...options
});

/**
 * Create a keyspace
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `create_branch`
 *
 *
 */
export const createKeyspace = <ThrowOnError extends boolean = false>(options: Options<CreateKeyspaceData, ThrowOnError>) => (options.client ?? client).post<CreateKeyspaceResponses, CreateKeyspaceErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/keyspaces',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a keyspace
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_branch`, `delete_production_branch`
 *
 *
 */
export const deleteKeyspace = <ThrowOnError extends boolean = false>(options: Options<DeleteKeyspaceData, ThrowOnError>) => (options.client ?? client).delete<DeleteKeyspaceResponses, DeleteKeyspaceErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}',
    ...options
});

/**
 * Get a keyspace
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`
 *
 *
 */
export const getKeyspace = <ThrowOnError extends boolean = false>(options: Options<GetKeyspaceData, ThrowOnError>) => (options.client ?? client).get<GetKeyspaceResponses, GetKeyspaceErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}',
    ...options
});

/**
 * Configure keyspace settings
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `create_branch`
 *
 *
 */
export const updateKeyspace = <ThrowOnError extends boolean = false>(options: Options<UpdateKeyspaceData, ThrowOnError>) => (options.client ?? client).patch<UpdateKeyspaceResponses, UpdateKeyspaceErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}',
    ...options
});

/**
 * Get keyspace rollout status
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`
 *
 *
 */
export const getKeyspaceRolloutStatus = <ThrowOnError extends boolean = false>(options: Options<GetKeyspaceRolloutStatusData, ThrowOnError>) => (options.client ?? client).get<GetKeyspaceRolloutStatusResponses, GetKeyspaceRolloutStatusErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status',
    ...options
});

/**
 * Get the VSchema for the keyspace
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 *
 */
export const getKeyspaceVschema = <ThrowOnError extends boolean = false>(options: Options<GetKeyspaceVschemaData, ThrowOnError>) => (options.client ?? client).get<GetKeyspaceVschemaResponses, GetKeyspaceVschemaErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema',
    ...options
});

/**
 * Update the VSchema for the keyspace
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_production_branch_vschema`, `write_branch_vschema`
 *
 *
 */
export const updateKeyspaceVschema = <ThrowOnError extends boolean = false>(options: Options<UpdateKeyspaceVschemaData, ThrowOnError>) => (options.client ?? client).patch<UpdateKeyspaceVschemaResponses, UpdateKeyspaceVschemaErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List cluster parameters
 *
 *         Returns the parameters for a branch. To update the parameters, use the "Upsert a change request" endpoint.
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const listParameters = <ThrowOnError extends boolean = false>(options: Options<ListParametersData, ThrowOnError>) => (options.client ?? client).get<ListParametersResponses, ListParametersErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/parameters',
    ...options
});

/**
 * List passwords
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const listPasswords = <ThrowOnError extends boolean = false>(options: Options<ListPasswordsData, ThrowOnError>) => (options.client ?? client).get<ListPasswordsResponses, ListPasswordsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/passwords',
    ...options
});

/**
 * Create a password
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const createPassword = <ThrowOnError extends boolean = false>(options: Options<CreatePasswordData, ThrowOnError>) => (options.client ?? client).post<CreatePasswordResponses, CreatePasswordErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/passwords',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a password
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_production_branch_password`, `delete_branch_password`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const deletePassword = <ThrowOnError extends boolean = false>(options: Options<DeletePasswordData, ThrowOnError>) => (options.client ?? client).delete<DeletePasswordResponses, DeletePasswordErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}',
    ...options
});

/**
 * Get a password
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const getPassword = <ThrowOnError extends boolean = false>(options: Options<GetPasswordData, ThrowOnError>) => (options.client ?? client).get<GetPasswordResponses, GetPasswordErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}',
    ...options
});

/**
 * Update a password
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const updatePassword = <ThrowOnError extends boolean = false>(options: Options<UpdatePasswordData, ThrowOnError>) => (options.client ?? client).patch<UpdatePasswordResponses, UpdatePasswordErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Renew a password
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const renewPassword = <ThrowOnError extends boolean = false>(options: Options<RenewPasswordData, ThrowOnError>) => (options.client ?? client).post<RenewPasswordResponses, RenewPasswordErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew',
    ...options
});

/**
 * Promote a branch
 *
 * Promotes a branch from development to production
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `connect_production_branch`, `promote_branches`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `promote_branches` |
 * | Database | `promote_branches` |
 */
export const promoteBranch = <ThrowOnError extends boolean = false>(options: Options<PromoteBranchData, ThrowOnError>) => (options.client ?? client).post<PromoteBranchResponses, PromoteBranchErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/promote',
    ...options
});

/**
 * List generated query patterns reports
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const listGeneratedQueryPatternsReports = <ThrowOnError extends boolean = false>(options: Options<ListGeneratedQueryPatternsReportsData, ThrowOnError>) => (options.client ?? client).get<ListGeneratedQueryPatternsReportsResponses, ListGeneratedQueryPatternsReportsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/query-patterns',
    ...options
});

/**
 * Create a new query patterns report
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const createQueryPatternsReport = <ThrowOnError extends boolean = false>(options: Options<CreateQueryPatternsReportData, ThrowOnError>) => (options.client ?? client).post<CreateQueryPatternsReportResponses, CreateQueryPatternsReportErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/query-patterns',
    ...options
});

/**
 * Delete a query patterns report
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const deleteQueryPatternsReport = <ThrowOnError extends boolean = false>(options: Options<DeleteQueryPatternsReportData, ThrowOnError>) => (options.client ?? client).delete<DeleteQueryPatternsReportResponses, DeleteQueryPatternsReportErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}',
    ...options
});

/**
 * Show the status of a query patterns report
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const getQueryPatternsReportStatus = <ThrowOnError extends boolean = false>(options: Options<GetQueryPatternsReportStatusData, ThrowOnError>) => (options.client ?? client).get<GetQueryPatternsReportStatusResponses, GetQueryPatternsReportStatusErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}',
    ...options
});

/**
 * Download a finished query patterns report
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const getQueryPatternsReport = <ThrowOnError extends boolean = false>(options: Options<GetQueryPatternsReportData, ThrowOnError>) => (options.client ?? client).get<unknown, GetQueryPatternsReportErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download',
    ...options
});

/**
 * Cancel a change request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const cancelBranchChangeRequest = <ThrowOnError extends boolean = false>(options: Options<CancelBranchChangeRequestData, ThrowOnError>) => (options.client ?? client).delete<CancelBranchChangeRequestResponses, CancelBranchChangeRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/resizes',
    ...options
});

/**
 * List roles
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const listRoles = <ThrowOnError extends boolean = false>(options: Options<ListRolesData, ThrowOnError>) => (options.client ?? client).get<ListRolesResponses, ListRolesErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/roles',
    ...options
});

/**
 * Create role credentials
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `create_production_branch_password`, `create_branch_password`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const createRole = <ThrowOnError extends boolean = false>(options: Options<CreateRoleData, ThrowOnError>) => (options.client ?? client).post<CreateRoleResponses, CreateRoleErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/roles',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the default postgres role
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const getDefaultRole = <ThrowOnError extends boolean = false>(options: Options<GetDefaultRoleData, ThrowOnError>) => (options.client ?? client).get<GetDefaultRoleResponses, GetDefaultRoleErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/roles/default',
    ...options
});

/**
 * Reset default credentials
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_production_branch_password`, `delete_branch_password`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const resetDefaultRole = <ThrowOnError extends boolean = false>(options: Options<ResetDefaultRoleData, ThrowOnError>) => (options.client ?? client).post<ResetDefaultRoleResponses, ResetDefaultRoleErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default',
    ...options
});

/**
 * Delete role credentials
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_production_branch_password`, `delete_branch_password`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const deleteRole = <ThrowOnError extends boolean = false>(options: Options<DeleteRoleData, ThrowOnError>) => (options.client ?? client).delete<DeleteRoleResponses, DeleteRoleErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get a role
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const getRole = <ThrowOnError extends boolean = false>(options: Options<GetRoleData, ThrowOnError>) => (options.client ?? client).get<GetRoleResponses, GetRoleErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}',
    ...options
});

/**
 * Update role name
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `create_production_branch_password`, `create_branch_password`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const updateRole = <ThrowOnError extends boolean = false>(options: Options<UpdateRoleData, ThrowOnError>) => (options.client ?? client).patch<UpdateRoleResponses, UpdateRoleErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reassign objects owned by one role to another role
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_production_branch_password`, `delete_branch_password`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const reassignRoleObjects = <ThrowOnError extends boolean = false>(options: Options<ReassignRoleObjectsData, ThrowOnError>) => (options.client ?? client).post<ReassignRoleObjectsResponses, ReassignRoleObjectsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Renew role expiration
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `create_production_branch_password`, `create_branch_password`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const renewRole = <ThrowOnError extends boolean = false>(options: Options<RenewRoleData, ThrowOnError>) => (options.client ?? client).post<RenewRoleResponses, RenewRoleErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew',
    ...options
});

/**
 * Reset a role's password
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_production_branch_password`, `delete_branch_password`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `manage_passwords`, `manage_production_branch_passwords` |
 * | Database | `manage_passwords`, `manage_production_branch_passwords` |
 * | Branch | `manage_passwords` |
 */
export const resetRole = <ThrowOnError extends boolean = false>(options: Options<ResetRoleData, ThrowOnError>) => (options.client ?? client).post<ResetRoleResponses, ResetRoleErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset',
    ...options
});

/**
 * Disable safe migrations for a branch
 *
 *
 *
 */
export const disableSafeMigrations = <ThrowOnError extends boolean = false>(options: Options<DisableSafeMigrationsData, ThrowOnError>) => (options.client ?? client).delete<DisableSafeMigrationsResponses, DisableSafeMigrationsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations',
    ...options
});

/**
 * Enable safe migrations for a branch
 *
 *
 *
 */
export const enableSafeMigrations = <ThrowOnError extends boolean = false>(options: Options<EnableSafeMigrationsData, ThrowOnError>) => (options.client ?? client).post<EnableSafeMigrationsResponses, EnableSafeMigrationsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations',
    ...options
});

/**
 * Get a branch schema
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const getBranchSchema = <ThrowOnError extends boolean = false>(options: Options<GetBranchSchemaData, ThrowOnError>) => (options.client ?? client).get<GetBranchSchemaResponses, GetBranchSchemaErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/schema',
    ...options
});

/**
 * Lint a branch schema
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_branch`, `delete_branch`, `create_branch`, `connect_production_branch`, `connect_branch`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 * | Branch | `read_branch` |
 */
export const lintBranchSchema = <ThrowOnError extends boolean = false>(options: Options<LintBranchSchemaData, ThrowOnError>) => (options.client ?? client).get<LintBranchSchemaResponses, LintBranchSchemaErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/branches/{branch}/schema/lint',
    ...options
});

/**
 * List IP restriction entries
 *
 *
 * ### Authorization
 * A   OAuth token must have at least one of the following   scopes in order to use this API endpoint:
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_databases` |
 * | Database | `read_database` |
 */
export const listDatabasePostgresCidrs = <ThrowOnError extends boolean = false>(options: Options<ListDatabasePostgresCidrsData, ThrowOnError>) => (options.client ?? client).get<ListDatabasePostgresCidrsResponses, ListDatabasePostgresCidrsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/cidrs',
    ...options
});

/**
 * Create an IP restriction entry
 *
 *
 * ### Authorization
 * A   OAuth token must have at least one of the following   scopes in order to use this API endpoint:
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const createDatabasePostgresCidr = <ThrowOnError extends boolean = false>(options: Options<CreateDatabasePostgresCidrData, ThrowOnError>) => (options.client ?? client).post<CreateDatabasePostgresCidrResponses, CreateDatabasePostgresCidrErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/cidrs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an IP restriction entry
 *
 *
 * ### Authorization
 * A   OAuth token must have at least one of the following   scopes in order to use this API endpoint:
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const deleteDatabasePostgresCidr = <ThrowOnError extends boolean = false>(options: Options<DeleteDatabasePostgresCidrData, ThrowOnError>) => (options.client ?? client).delete<DeleteDatabasePostgresCidrResponses, DeleteDatabasePostgresCidrErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/cidrs/{id}',
    ...options
});

/**
 * Get an IP restriction entry
 *
 *
 * ### Authorization
 * A   OAuth token must have at least one of the following   scopes in order to use this API endpoint:
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_databases` |
 * | Database | `read_database` |
 */
export const getDatabasePostgresCidr = <ThrowOnError extends boolean = false>(options: Options<GetDatabasePostgresCidrData, ThrowOnError>) => (options.client ?? client).get<GetDatabasePostgresCidrResponses, GetDatabasePostgresCidrErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/cidrs/{id}',
    ...options
});

/**
 * Update an IP restriction entry
 *
 *
 * ### Authorization
 * A   OAuth token must have at least one of the following   scopes in order to use this API endpoint:
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const updateDatabasePostgresCidr = <ThrowOnError extends boolean = false>(options: Options<UpdateDatabasePostgresCidrData, ThrowOnError>) => (options.client ?? client).put<UpdateDatabasePostgresCidrResponses, UpdateDatabasePostgresCidrErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/cidrs/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the deploy queue
 *
 * The deploy queue returns the current list of deploy requests in the order they will be deployed.
 *
 */
export const getDeployQueue = <ThrowOnError extends boolean = false>(options: Options<GetDeployQueueData, ThrowOnError>) => (options.client ?? client).get<GetDeployQueueResponses, GetDeployQueueErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-queue',
    ...options
});

/**
 * List deploy requests
 *
 * List deploy requests for a database
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_deploy_requests` |
 * | Database | `read_deploy_requests` |
 */
export const listDeployRequests = <ThrowOnError extends boolean = false>(options: Options<ListDeployRequestsData, ThrowOnError>) => (options.client ?? client).get<ListDeployRequestsResponses, ListDeployRequestsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests',
    ...options
});

/**
 * Create a deploy request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_requests`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_deploy_requests` |
 * | Database | `write_deploy_requests` |
 */
export const createDeployRequest = <ThrowOnError extends boolean = false>(options: Options<CreateDeployRequestData, ThrowOnError>) => (options.client ?? client).post<CreateDeployRequestResponses, CreateDeployRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get a deploy request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_deploy_requests` |
 * | Database | `read_deploy_requests` |
 */
export const getDeployRequest = <ThrowOnError extends boolean = false>(options: Options<GetDeployRequestData, ThrowOnError>) => (options.client ?? client).get<GetDeployRequestResponses, GetDeployRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}',
    ...options
});

/**
 * Close a deploy request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_deploy_requests` |
 * | Database | `write_deploy_requests` |
 */
export const closeDeployRequest = <ThrowOnError extends boolean = false>(options: Options<CloseDeployRequestData, ThrowOnError>) => (options.client ?? client).patch<CloseDeployRequestResponses, CloseDeployRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Complete a gated deploy request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `deploy_deploy_requests` |
 * | Database | `deploy_deploy_requests` |
 */
export const completeGatedDeployRequest = <ThrowOnError extends boolean = false>(options: Options<CompleteGatedDeployRequestData, ThrowOnError>) => (options.client ?? client).post<CompleteGatedDeployRequestResponses, CompleteGatedDeployRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy',
    ...options
});

/**
 * Update auto-apply for deploy request
 *
 * Enables or disabled the auto-apply setting for a deploy request
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `deploy_deploy_requests` |
 * | Database | `deploy_deploy_requests` |
 */
export const updateAutoApply = <ThrowOnError extends boolean = false>(options: Options<UpdateAutoApplyData, ThrowOnError>) => (options.client ?? client).put<UpdateAutoApplyResponses, UpdateAutoApplyErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update auto-delete branch for deploy request
 *
 * Enables or disabled the auto-delete branch setting for a deploy request
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `deploy_deploy_requests` |
 * | Database | `deploy_deploy_requests` |
 */
export const updateAutoDeleteBranch = <ThrowOnError extends boolean = false>(options: Options<UpdateAutoDeleteBranchData, ThrowOnError>) => (options.client ?? client).put<UpdateAutoDeleteBranchResponses, UpdateAutoDeleteBranchErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cancel a queued deploy request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `deploy_deploy_requests` |
 * | Database | `deploy_deploy_requests` |
 */
export const cancelDeployRequest = <ThrowOnError extends boolean = false>(options: Options<CancelDeployRequestData, ThrowOnError>) => (options.client ?? client).post<CancelDeployRequestResponses, CancelDeployRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel',
    ...options
});

/**
 * Complete an errored deploy
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `deploy_deploy_requests` |
 * | Database | `deploy_deploy_requests` |
 */
export const completeErroredDeploy = <ThrowOnError extends boolean = false>(options: Options<CompleteErroredDeployData, ThrowOnError>) => (options.client ?? client).post<CompleteErroredDeployResponses, CompleteErroredDeployErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy',
    ...options
});

/**
 * Queue a deploy request
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `deploy_deploy_requests` |
 * | Database | `deploy_deploy_requests` |
 */
export const queueDeployRequest = <ThrowOnError extends boolean = false>(options: Options<QueueDeployRequestData, ThrowOnError>) => (options.client ?? client).post<QueueDeployRequestResponses, QueueDeployRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get a deployment
 *
 * Get the deployment for a deploy request
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_deploy_requests` |
 * | Database | `read_deploy_requests` |
 */
export const getDeployment = <ThrowOnError extends boolean = false>(options: Options<GetDeploymentData, ThrowOnError>) => (options.client ?? client).get<GetDeploymentResponses, GetDeploymentErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment',
    ...options
});

/**
 * List deploy operations
 *
 * List deploy operations for a deploy request
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_deploy_requests` |
 * | Database | `read_deploy_requests` |
 */
export const listDeployOperations = <ThrowOnError extends boolean = false>(options: Options<ListDeployOperationsData, ThrowOnError>) => (options.client ?? client).get<ListDeployOperationsResponses, ListDeployOperationsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/operations',
    ...options
});

/**
 * Complete a revert
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `deploy_deploy_requests` |
 * | Database | `deploy_deploy_requests` |
 */
export const completeRevert = <ThrowOnError extends boolean = false>(options: Options<CompleteRevertData, ThrowOnError>) => (options.client ?? client).post<CompleteRevertResponses, CompleteRevertErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/revert',
    ...options
});

/**
 * List deploy request reviews
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_deploy_requests` |
 * | Database | `read_deploy_requests` |
 */
export const listDeployRequestReviews = <ThrowOnError extends boolean = false>(options: Options<ListDeployRequestReviewsData, ThrowOnError>) => (options.client ?? client).get<ListDeployRequestReviewsResponses, ListDeployRequestReviewsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews',
    ...options
});

/**
 * Review a deploy request
 *
 * Review a deploy request by either approving or commenting on the deploy request
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `approve_deploy_request`, `review_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `approve_deploy_requests` |
 * | Database | `approve_deploy_requests` |
 */
export const reviewDeployRequest = <ThrowOnError extends boolean = false>(options: Options<ReviewDeployRequestData, ThrowOnError>) => (options.client ?? client).post<ReviewDeployRequestResponses, ReviewDeployRequestErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Skip revert period
 *
 * Skips the revert period for a deploy request
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `deploy_deploy_requests` |
 * | Database | `deploy_deploy_requests` |
 */
export const skipRevertPeriod = <ThrowOnError extends boolean = false>(options: Options<SkipRevertPeriodData, ThrowOnError>) => (options.client ?? client).post<SkipRevertPeriodResponses, SkipRevertPeriodErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert',
    ...options
});

/**
 * Get deploy request throttler configurations
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_deploy_requests` |
 * | Database | `read_deploy_requests` |
 */
export const getDeployRequestThrottler = <ThrowOnError extends boolean = false>(options: Options<GetDeployRequestThrottlerData, ThrowOnError>) => (options.client ?? client).get<GetDeployRequestThrottlerResponses, GetDeployRequestThrottlerErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler',
    ...options
});

/**
 * Update deploy request throttler configurations
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `deploy_deploy_requests` |
 * | Database | `deploy_deploy_requests` |
 */
export const updateDeployRequestThrottler = <ThrowOnError extends boolean = false>(options: Options<UpdateDeployRequestThrottlerData, ThrowOnError>) => (options.client ?? client).patch<UpdateDeployRequestThrottlerResponses, UpdateDeployRequestThrottlerErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List read-only regions
 *
 * List read-only regions for the database's default branch
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_database`, `delete_database`, `write_database`, `read_branch`, `delete_branch`, `create_branch`, `delete_production_branch`, `connect_branch`, `connect_production_branch`, `delete_branch_password`, `delete_production_branch_password`, `read_deploy_request`, `create_deploy_request`, `approve_deploy_request`, `read_comment`, `create_comment`, `restore_backup`, `restore_production_branch_backup`, `read_backups`, `write_backups`, `delete_backups`, `delete_production_branch_backups`, `write_branch_vschema`, `write_production_branch_vschema`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_branches` |
 * | Database | `read_branches` |
 */
export const listReadOnlyRegions = <ThrowOnError extends boolean = false>(options: Options<ListReadOnlyRegionsData, ThrowOnError>) => (options.client ?? client).get<ListReadOnlyRegionsResponses, ListReadOnlyRegionsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/read-only-regions',
    ...options
});

/**
 * List database regions
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_database`, `delete_database`, `write_database`, `read_branch`, `delete_branch`, `create_branch`, `delete_production_branch`, `connect_branch`, `connect_production_branch`, `delete_branch_password`, `delete_production_branch_password`, `read_deploy_request`, `create_deploy_request`, `approve_deploy_request`, `read_comment`, `create_comment`, `restore_backup`, `restore_production_branch_backup`, `read_backups`, `write_backups`, `delete_backups`, `delete_production_branch_backups`, `write_branch_vschema`, `write_production_branch_vschema`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_databases` |
 * | Database | `read_database` |
 */
export const listDatabaseRegions = <ThrowOnError extends boolean = false>(options: Options<ListDatabaseRegionsData, ThrowOnError>) => (options.client ?? client).get<ListDatabaseRegionsResponses, ListDatabaseRegionsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/regions',
    ...options
});

/**
 * Get database throttler configurations
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_databases` |
 * | Database | `read_database` |
 */
export const getDatabaseThrottler = <ThrowOnError extends boolean = false>(options: Options<GetDatabaseThrottlerData, ThrowOnError>) => (options.client ?? client).get<GetDatabaseThrottlerResponses, GetDatabaseThrottlerErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/throttler',
    ...options
});

/**
 * Update database throttler configurations
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_deploy_request`, `create_deploy_request`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `deploy_deploy_requests` |
 * | Database | `deploy_deploy_requests` |
 */
export const updateDatabaseThrottler = <ThrowOnError extends boolean = false>(options: Options<UpdateDatabaseThrottlerData, ThrowOnError>) => (options.client ?? client).patch<UpdateDatabaseThrottlerResponses, UpdateDatabaseThrottlerErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/throttler',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List webhooks
 *
 * List webhooks for a database
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_databases` |
 * | Database | `read_database` |
 */
export const listWebhooks = <ThrowOnError extends boolean = false>(options: Options<ListWebhooksData, ThrowOnError>) => (options.client ?? client).get<ListWebhooksResponses, ListWebhooksErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/webhooks',
    ...options
});

/**
 * Create a webhook
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const createWebhook = <ThrowOnError extends boolean = false>(options: Options<CreateWebhookData, ThrowOnError>) => (options.client ?? client).post<CreateWebhookResponses, CreateWebhookErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/webhooks',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a webhook
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const deleteWebhook = <ThrowOnError extends boolean = false>(options: Options<DeleteWebhookData, ThrowOnError>) => (options.client ?? client).delete<DeleteWebhookResponses, DeleteWebhookErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/webhooks/{id}',
    ...options
});

/**
 * Get a webhook
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_databases` |
 * | Database | `read_database` |
 */
export const getWebhook = <ThrowOnError extends boolean = false>(options: Options<GetWebhookData, ThrowOnError>) => (options.client ?? client).get<GetWebhookResponses, GetWebhookErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/webhooks/{id}',
    ...options
});

/**
 * Update a webhook
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const updateWebhook = <ThrowOnError extends boolean = false>(options: Options<UpdateWebhookData, ThrowOnError>) => (options.client ?? client).patch<UpdateWebhookResponses, UpdateWebhookErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/webhooks/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Test a webhook
 *
 * Sends a test event to the webhook
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_database`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_databases` |
 * | Database | `write_database` |
 */
export const testWebhook = <ThrowOnError extends boolean = false>(options: Options<TestWebhookData, ThrowOnError>) => (options.client ?? client).post<TestWebhookResponses, TestWebhookErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/webhooks/{id}/test',
    ...options
});

/**
 * List workflows
 *
 *
 *
 */
export const listWorkflows = <ThrowOnError extends boolean = false>(options: Options<ListWorkflowsData, ThrowOnError>) => (options.client ?? client).get<ListWorkflowsResponses, ListWorkflowsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows',
    ...options
});

/**
 * Create a workflow
 *
 *
 *
 */
export const createWorkflow = <ThrowOnError extends boolean = false>(options: Options<CreateWorkflowData, ThrowOnError>) => (options.client ?? client).post<CreateWorkflowResponses, CreateWorkflowErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cancel a workflow
 *
 *
 *
 */
export const workflowCancel = <ThrowOnError extends boolean = false>(options: Options<WorkflowCancelData, ThrowOnError>) => (options.client ?? client).delete<WorkflowCancelResponses, WorkflowCancelErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows/{number}',
    ...options
});

/**
 * Get a workflow
 *
 *
 *
 */
export const getWorkflow = <ThrowOnError extends boolean = false>(options: Options<GetWorkflowData, ThrowOnError>) => (options.client ?? client).get<GetWorkflowResponses, GetWorkflowErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows/{number}',
    ...options
});

/**
 * Complete a workflow
 *
 *
 *
 */
export const workflowComplete = <ThrowOnError extends boolean = false>(options: Options<WorkflowCompleteData, ThrowOnError>) => (options.client ?? client).patch<WorkflowCompleteResponses, WorkflowCompleteErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows/{number}/complete',
    ...options
});

/**
 * Cutover traffic
 *
 *
 *
 */
export const workflowCutover = <ThrowOnError extends boolean = false>(options: Options<WorkflowCutoverData, ThrowOnError>) => (options.client ?? client).patch<WorkflowCutoverResponses, WorkflowCutoverErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows/{number}/cutover',
    ...options
});

/**
 * Retry a failed workflow
 *
 *
 *
 */
export const workflowRetry = <ThrowOnError extends boolean = false>(options: Options<WorkflowRetryData, ThrowOnError>) => (options.client ?? client).patch<WorkflowRetryResponses, WorkflowRetryErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows/{number}/retry',
    ...options
});

/**
 * Reverse traffic cutover
 *
 *
 *
 */
export const workflowReverseCutover = <ThrowOnError extends boolean = false>(options: Options<WorkflowReverseCutoverData, ThrowOnError>) => (options.client ?? client).patch<WorkflowReverseCutoverResponses, WorkflowReverseCutoverErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover',
    ...options
});

/**
 * Reverse traffic
 *
 *
 *
 */
export const workflowReverseTraffic = <ThrowOnError extends boolean = false>(options: Options<WorkflowReverseTrafficData, ThrowOnError>) => (options.client ?? client).patch<WorkflowReverseTrafficResponses, WorkflowReverseTrafficErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic',
    ...options
});

/**
 * Switch primary traffic
 *
 *
 *
 */
export const workflowSwitchPrimaries = <ThrowOnError extends boolean = false>(options: Options<WorkflowSwitchPrimariesData, ThrowOnError>) => (options.client ?? client).patch<WorkflowSwitchPrimariesResponses, WorkflowSwitchPrimariesErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries',
    ...options
});

/**
 * Switch replica traffic
 *
 *
 *
 */
export const workflowSwitchReplicas = <ThrowOnError extends boolean = false>(options: Options<WorkflowSwitchReplicasData, ThrowOnError>) => (options.client ?? client).patch<WorkflowSwitchReplicasResponses, WorkflowSwitchReplicasErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas',
    ...options
});

/**
 * Verify workflow data
 *
 *
 *
 */
export const verifyWorkflow = <ThrowOnError extends boolean = false>(options: Options<VerifyWorkflowData, ThrowOnError>) => (options.client ?? client).patch<VerifyWorkflowResponses, VerifyWorkflowErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/databases/{database}/workflows/{number}/verify-data',
    ...options
});

/**
 * Get invoices
 *
 * Get the invoices for an organization
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_invoices`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_invoices` |
 */
export const listInvoices = <ThrowOnError extends boolean = false>(options: Options<ListInvoicesData, ThrowOnError>) => (options.client ?? client).get<ListInvoicesResponses, ListInvoicesErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/invoices',
    ...options
});

/**
 * Get an invoice
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_invoices`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_invoices` |
 */
export const getInvoice = <ThrowOnError extends boolean = false>(options: Options<GetInvoiceData, ThrowOnError>) => (options.client ?? client).get<GetInvoiceResponses, GetInvoiceErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/invoices/{id}',
    ...options
});

/**
 * Get invoice line items
 *
 * Get the line items for an invoice
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_invoices`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_invoices` |
 */
export const getInvoiceLineItems = <ThrowOnError extends boolean = false>(options: Options<GetInvoiceLineItemsData, ThrowOnError>) => (options.client ?? client).get<GetInvoiceLineItemsResponses, GetInvoiceLineItemsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/invoices/{id}/line-items',
    ...options
});

/**
 * List organization members
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_organization` |
 */
export const listOrganizationMembers = <ThrowOnError extends boolean = false>(options: Options<ListOrganizationMembersData, ThrowOnError>) => (options.client ?? client).get<ListOrganizationMembersResponses, ListOrganizationMembersErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/members',
    ...options
});

/**
 * Remove a member from an organization
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_organization` |
 */
export const removeOrganizationMember = <ThrowOnError extends boolean = false>(options: Options<RemoveOrganizationMemberData, ThrowOnError>) => (options.client ?? client).delete<RemoveOrganizationMemberResponses, RemoveOrganizationMemberErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/members/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get an organization member
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_organization` |
 */
export const getOrganizationMembership = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationMembershipData, ThrowOnError>) => (options.client ?? client).get<GetOrganizationMembershipResponses, GetOrganizationMembershipErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/members/{id}',
    ...options
});

/**
 * Update organization member role
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_organization` |
 */
export const updateOrganizationMembership = <ThrowOnError extends boolean = false>(options: Options<UpdateOrganizationMembershipData, ThrowOnError>) => (options.client ?? client).patch<UpdateOrganizationMembershipResponses, UpdateOrganizationMembershipErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/members/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List OAuth applications
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_oauth_applications`
 *
 *
 */
export const listOauthApplications = <ThrowOnError extends boolean = false>(options: Options<ListOauthApplicationsData, ThrowOnError>) => (options.client ?? client).get<ListOauthApplicationsResponses, ListOauthApplicationsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/oauth-applications',
    ...options
});

/**
 * Get an OAuth application
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_oauth_applications`
 *
 *
 */
export const getOauthApplication = <ThrowOnError extends boolean = false>(options: Options<GetOauthApplicationData, ThrowOnError>) => (options.client ?? client).get<GetOauthApplicationResponses, GetOauthApplicationErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/oauth-applications/{application_id}',
    ...options
});

/**
 * List OAuth tokens
 *
 * List OAuth tokens created by an OAuth application
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_oauth_tokens`
 *
 *
 */
export const listOauthTokens = <ThrowOnError extends boolean = false>(options: Options<ListOauthTokensData, ThrowOnError>) => (options.client ?? client).get<ListOauthTokensResponses, ListOauthTokensErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/oauth-applications/{application_id}/tokens',
    ...options
});

/**
 * Delete an OAuth token
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_oauth_tokens`
 *
 *
 */
export const deleteOauthToken = <ThrowOnError extends boolean = false>(options: Options<DeleteOauthTokenData, ThrowOnError>) => (options.client ?? client).delete<DeleteOauthTokenResponses, DeleteOauthTokenErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}',
    ...options
});

/**
 * Get an OAuth token
 *
 *
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_oauth_tokens`
 *
 *
 */
export const getOauthToken = <ThrowOnError extends boolean = false>(options: Options<GetOauthTokenData, ThrowOnError>) => (options.client ?? client).get<GetOauthTokenResponses, GetOauthTokenErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}',
    ...options
});

/**
 * Create or renew an OAuth token
 *
 * Create an OAuth token from an authorization grant code, or refresh an OAuth token from a refresh token
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_oauth_tokens`
 *
 *
 */
export const createOauthToken = <ThrowOnError extends boolean = false>(options: Options<CreateOauthTokenData, ThrowOnError>) => (options.client ?? client).post<CreateOauthTokenResponses, CreateOauthTokenErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/oauth-applications/{id}/token',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List regions for an organization
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | User | `read_organizations` |
 * | Organization | `read_organization` |
 */
export const listRegionsForOrganization = <ThrowOnError extends boolean = false>(options: Options<ListRegionsForOrganizationData, ThrowOnError>) => (options.client ?? client).get<ListRegionsForOrganizationResponses, ListRegionsForOrganizationErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/regions',
    ...options
});

/**
 * List service tokens
 *
 * List service tokens for an organization.
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_service_tokens`
 *
 *
 */
export const listServiceTokens = <ThrowOnError extends boolean = false>(options: Options<ListServiceTokensData, ThrowOnError>) => (options.client ?? client).get<ListServiceTokensResponses, ListServiceTokensErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/service-tokens',
    ...options
});

/**
 * Create a service token
 *
 * Create a new service token for the organization.
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_service_tokens`
 *
 *
 */
export const createServiceToken = <ThrowOnError extends boolean = false>(options: Options<CreateServiceTokenData, ThrowOnError>) => (options.client ?? client).post<CreateServiceTokenResponses, CreateServiceTokenErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/service-tokens',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a service token
 *
 * Delete a service token from the organization.
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `delete_service_tokens`
 *
 *
 */
export const deleteServiceToken = <ThrowOnError extends boolean = false>(options: Options<DeleteServiceTokenData, ThrowOnError>) => (options.client ?? client).delete<DeleteServiceTokenResponses, DeleteServiceTokenErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/service-tokens/{id}',
    ...options
});

/**
 * Get a service token
 *
 * Get information about a service token.
 * ### Authorization
 * A service token   must have at least one of the following access   in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_service_tokens`
 *
 *
 */
export const getServiceToken = <ThrowOnError extends boolean = false>(options: Options<GetServiceTokenData, ThrowOnError>) => (options.client ?? client).get<GetServiceTokenResponses, GetServiceTokenErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/service-tokens/{id}',
    ...options
});

/**
 * List teams in an organization
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_organization` |
 */
export const listOrganizationTeams = <ThrowOnError extends boolean = false>(options: Options<ListOrganizationTeamsData, ThrowOnError>) => (options.client ?? client).get<ListOrganizationTeamsResponses, ListOrganizationTeamsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/teams',
    ...options
});

/**
 * Create an organization team
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_teams`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_organization` |
 */
export const createOrganizationTeam = <ThrowOnError extends boolean = false>(options: Options<CreateOrganizationTeamData, ThrowOnError>) => (options.client ?? client).post<CreateOrganizationTeamResponses, CreateOrganizationTeamErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/teams',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an organization team
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_teams`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_organization` |
 */
export const deleteOrganizationTeam = <ThrowOnError extends boolean = false>(options: Options<DeleteOrganizationTeamData, ThrowOnError>) => (options.client ?? client).delete<DeleteOrganizationTeamResponses, DeleteOrganizationTeamErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/teams/{team}',
    ...options
});

/**
 * Get an organization team
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_organization` |
 */
export const getOrganizationTeam = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationTeamData, ThrowOnError>) => (options.client ?? client).get<GetOrganizationTeamResponses, GetOrganizationTeamErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/teams/{team}',
    ...options
});

/**
 * Update an organization team
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_teams`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_organization` |
 */
export const updateOrganizationTeam = <ThrowOnError extends boolean = false>(options: Options<UpdateOrganizationTeamData, ThrowOnError>) => (options.client ?? client).patch<UpdateOrganizationTeamResponses, UpdateOrganizationTeamErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/teams/{team}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List team members
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_organization` |
 */
export const listOrganizationTeamMembers = <ThrowOnError extends boolean = false>(options: Options<ListOrganizationTeamMembersData, ThrowOnError>) => (options.client ?? client).get<ListOrganizationTeamMembersResponses, ListOrganizationTeamMembersErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/teams/{team}/members',
    ...options
});

/**
 * Add a member to a team
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_teams`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_organization` |
 */
export const addOrganizationTeamMember = <ThrowOnError extends boolean = false>(options: Options<AddOrganizationTeamMemberData, ThrowOnError>) => (options.client ?? client).post<AddOrganizationTeamMemberResponses, AddOrganizationTeamMemberErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/teams/{team}/members',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove a member from a team
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `write_teams`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `write_organization` |
 */
export const removeOrganizationTeamMember = <ThrowOnError extends boolean = false>(options: Options<RemoveOrganizationTeamMemberData, ThrowOnError>) => (options.client ?? client).delete<RemoveOrganizationTeamMemberResponses, RemoveOrganizationTeamMemberErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/teams/{team}/members/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get a team member
 *
 *
 * ### Authorization
 * A service token or OAuth token must have at least one of the following access or scopes in order to use this API endpoint:
 *
 * **Service Token Accesses**
 * `read_organization`
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | Organization | `read_organization` |
 */
export const getOrganizationTeamMember = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationTeamMemberData, ThrowOnError>) => (options.client ?? client).get<GetOrganizationTeamMemberResponses, GetOrganizationTeamMemberErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/organizations/{organization}/teams/{team}/members/{id}',
    ...options
});

/**
 * List public regions
 *
 * Endpoint is available without authentication.
 *
 */
export const listPublicRegions = <ThrowOnError extends boolean = false>(options?: Options<ListPublicRegionsData, ThrowOnError>) => (options?.client ?? client).get<ListPublicRegionsResponses, ListPublicRegionsErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/regions',
    ...options
});

/**
 * Get current user
 *
 * Get the user associated with this service token
 * ### Authorization
 * A   OAuth token must have at least one of the following   scopes in order to use this API endpoint:
 *
 * **OAuth Scopes**
 *
 * | Resource | Scopes |
 * | :------- | :---------- |
 * | User | `read_user` |
 */
export const getCurrentUser = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentUserData, ThrowOnError>) => (options?.client ?? client).get<GetCurrentUserResponses, GetCurrentUserErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/user',
    ...options
});
